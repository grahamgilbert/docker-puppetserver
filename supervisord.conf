[unix_http_server]
file=/var/run/supervisor.sock   ; (the path to the socket file)
chmod=0700                       ; sockef file mode (default 0700)

[supervisord]
logfile=/tmp/supervisord.log ; (main log file;default $CWD/supervisord.log)
logfile_maxbytes=50MB        ; (max main logfile bytes b4 rotation;default 50MB)
logfile_backups=10           ; (num of main logfile rotation backups;default 10)
loglevel=info                ; (log level;default info; others: debug,warn,trace)
pidfile=/tmp/supervisord.pid ; (supervisord pidfile;default supervisord.pid)
nodaemon=false               ; (start in foreground if true;default false)
minfds=1024                  ; (min. avail startup file descriptors;default 1024)
minprocs=200                 ; (min. avail process descriptors;default 200)

; the below section must remain in the config file for RPC
; (supervisorctl/web interface) to work, additional interfaces may be
; added by defining them in separate rpcinterface: sections
[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock ; use a unix:// URL  for a unix socket

[program:conf]
command=/bin/bash /run.sh
priority=100
autostart=true
autorestart=unexpected
startretries=0
startsecs=0


[program:puppetserver]
command=/usr/sbin/service puppetserver start
priority=500
stdout_events_enabled=true
stderr_events_enabled=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=500
autostart=true
autorestart=unexpected
startsecs=0

[program:taillog]
command=/usr/bin/tail -f /var/log/puppetlabs/puppetserver/puppetserver.log
priority=600
stdout_events_enabled=true
stderr_events_enabled=true
stdout_logfile=/var/log/puppetlabs/puppetserver/puppetserver.log
stdout_logfile_maxbytes=500
